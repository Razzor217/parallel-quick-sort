#pragma once

/*
 * - normal interface + configurable interface
 * - configurable helper classes 
 *     - (de-)activate sorted checking
 *     - change base case size
 *     - ...
 * - separate sequential implementation
 * - base case sorter depending on size
 *     - small: insertion sort
 *     - medium: sequential quick sort
 * - tasks? barrier synch?
 */

namespace parallel
{

}